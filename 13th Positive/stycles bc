
.missionsStatement {
  text-align: center;
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  background-color: var(--primary-color);
  box-shadow: var(--shadow);
  transition: all ease-in-out 0.45s;
}

.missionsStatement p {
  font-size: 3.5rem;
  line-height: 1.2;
  letter-spacing: 1px;
  color: var(--background-light);
  font-family: var(--font-family-body);
  font-weight: bold;
  margin-top: 1rem;
  transition: all 0.3s ease;
}

.missionsStatement p .highlight {
  font-size: 3.5rem; /* Highlighted larger text */
  color: var(--accent-color); /* Accent color for special emphasis */
  font-weight: 900;
}
.missionsStatement p .punctiation {
  font-size: 4rem;
  color: #9b9999;
  font-weight: bold;
}